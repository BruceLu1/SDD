# 2025-12-11 工作摘要

- 建立 FastAPI Clean Architecture 專案骨架（core/domain/application/infrastructure/presentation 層與 __init__.py）
- 新增設定、枚舉與例外：app/core/config.py、enums/user_role.py、exceptions *(domain/application/infrastructure)*
- 實作 Domain/UseCase：Email value object、User entity、UserRepository 介面、重複檢查規則、Create/List/GetByEmail 用例與 DTO
- Infrastructure：SQLAlchemy UserModel 與 UserRepositoryImpl，簡易 HTTP client 範例
- Presentation：Pydantic schema、UserController、router factory；main.py DI 組裝與 /health
- 補上缺少的 email-validator 套件並更新 requirements.txt；重新執行 uvicorn app.main:app --reload
- 啟用 Swagger UI，設定 docs_url=/swagger，停用 redoc
